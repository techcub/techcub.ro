---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/sections/Header.astro';
import Footer from '../components/sections/Footer.astro';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  noindex?: boolean;
  titleTemplate?: boolean;
}

const { title, description, ogImage, noindex, titleTemplate } = Astro.props;
---

<BaseLayout title={title} description={description} ogImage={ogImage} noindex={noindex} titleTemplate={titleTemplate}>
  <Header />
  <main>
    <slot />
  </main>
  <Footer />
</BaseLayout>
