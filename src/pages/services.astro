---
import ServicesOverviewView from '@/components/pages/views/ServicesOverviewView.astro';
import { defaultLocale } from '@/i18n';

// The Services page for the default locale. Additional locales are served by
// `src/pages/[locale]/services.astro`, which renders the same view.
---

<ServicesOverviewView locale={defaultLocale} />
